version: '3'
services:
    api-server:
        image: petio
        ports:
            - '32600:32600'
            - '32601:32601'
            - '32650:32650'
            - '32700:32700'
        networks:
            - mern-app
        depends_on:
            - mongo
        environment:
            - 'REACT_APP_DB_DEFAULT=mongodb://mongo:27017'

    mongo:
        image: mongo:latest
        container_name: 'mongo'
        hostname: mongo
        ports:
            - '27017:27017'
        networks:
            - mern-app
        volumes:
            - mongo-data:/data/db
networks:
    mern-app:
        driver: bridge
volumes:
    mongo-data:
        driver: local
